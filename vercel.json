{
  "version": 2,
  "name": "brazmar-noticias",
  "public": true,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/",
      "dest": "server.js"
    },
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    },
    {
      "src": "/(.*)",
      "dest": "public/$1"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "buildCommand": "npm install && python -m pip install feedparser -t ./python-packages || echo 'Python dependencies instaladas'",
  "installCommand": "npm install && if [ -f requirements.txt ]; then pip install -r requirements.txt -t ./python-packages; fi"
}